# Reusable workflow for PRs; to eject, you can replace this file with
# https://github.com/fleatomarket/bluetileproject/blob/main/.github/workflows/pr.yml
name: Pull Request

on: [pull_request]

jobs:
  build:
    uses: fleatomarket/bluetileproject/.github/workflows/pr.yml@main
